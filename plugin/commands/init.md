---
description: Bootstrap the specced agentic coding setup into this repo via a stack-aware interview.
argument-hint: "[--minimal]"
---

Set up **specced** in the current repository.

Use the `specced-bootstrap` skill and follow it end to end: preflight, detect the
stack, interview me, run the `specced` CLI for the mechanical scaffolding, then
author the project-specific CONSTITUTION, rules, code-review dimensions, MCP
servers, and Makefile verification targets.

Arguments (optional): `$ARGUMENTS`
- `--minimal` → install only the engine + agents + managed blocks, then author all
  Layer-2 content by hand during the interview (skip the CLI's skeleton files).

Begin with the preflight step now.
